export interface InputFieldProps {
    id: string;
    label: string;
    placeholder: string;
    type?: string;
    required?: boolean;
}

export interface SocialLoginButtonProps {
    icon: React.ReactNode;
    text: string;
}

